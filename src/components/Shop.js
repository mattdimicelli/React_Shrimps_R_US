import ProductSelection from './ProductSelection';
import NavBar from './NavBar';

function Shop(props) {
    return (
        <div>
            <NavBar />
            <ProductSelection />
        </div>
    );
}

export default Shop;